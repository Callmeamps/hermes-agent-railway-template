services:
  hermes:
    build: .
    ports:
      - "8080:8080"
      - "9119:9119"
    environment:
      - PORT=8080
      - ADMIN_USERNAME=admin
      - ADMIN_PASSWORD=admin
      - DASHBOARD_PORT=9119
    volumes:
      - hermes-data:/data

volumes:
  hermes-data:
